# ربط أعمال تطبيق سند بالموقع

يعرض الموقع الأعمال المنشورة من تطبيق «سند — لوحة النشر» عبر دالة Netlify عامة على المسار `/api/works`.

## الإعداد المطلوب في Netlify

أضف متغير البيئة التالي من: **Project configuration → Environment variables**:

| الاسم | القيمة |
|---|---|
| `SANAD_API_BASE_URL` | عنوان خادم تطبيق سند المنشور، بدون شرطة مائلة في النهاية. مثال: `https://api.example.com` |

لا تضع رمز دخول أو كلمة مرور في هذا المتغير. يجب أن يشير العنوان إلى الخادم الذي يوفر المسار العام `/api/trpc/works.publicList`.

بعد حفظ المتغير، أعد النشر من **Deploys → Trigger deploy → Deploy site**. عند الضغط على «نشر الآن» في التطبيق، سيظهر العمل المنشور في قسم «أعمال منشورة حديثاً» بالموقع خلال دقيقة على الأكثر.

